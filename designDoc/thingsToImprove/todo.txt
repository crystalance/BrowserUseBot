1. persistent profile  -- DONE (profile dir renamed to browser-use-user-data-dir-main so browser-use stops copying it to a temp dir)
2. import skills  -- DONE (skills/*.md, auto-match on /new, /skills, /skill add, send .md file; sample xiaohongshu-search.md)
3. gpt-5.4 emits malformed JSON ("trailing characters") -> agent fails 5x and stops. Try gpt-4o or add fallback_llm.
4. screenshot/click timeouts on slow sites (小红书 creator pages) -- consider raising CDP timeouts.
5. AWS: noVNC login handoff (see designDoc/deployToAWS) + bind 0.0.0.0 + PUBLIC_HOST + URL token.
6. the login window is not optimized for the phone
7. cant finish the original intended question
8. should accelerate the whole process
9. automate the deploy process
10. should add skill to automate regular tasks  -- DONE
11. progress heartbeat: push per-step "Next goal" to Telegram so long tasks don't look frozen (9-min silence after Resuming)

====================================================================
ENGINEERING IMPROVEMENTS (resume-oriented, for US 27 NG SWE / AI Eng)
Prioritized. Each notes WHAT IT DEMONSTRATES to a reviewer.
====================================================================

--- A. OBSERVABILITY (highest signal for "production" thinking) --- [important]
A1. LLM tracing: integrate Langfuse or LangSmith — capture every LLM call
    (prompt, tokens, latency, cost, model) per run/step. One dashboard to see
    where time & $ go.  [Demonstrates: LLM observability, cost awareness]
A2. Structured logs: emit JSON logs (run_id, step, action, latency_ms, tokens)
    instead of plain text; ship to a store you can query.  [Demonstrates: log design]
A3. Metrics + dashboard: Prometheus counters/histograms (steps/run, latency,
    success_rate, tokens, 429s) + a Grafana panel.  [Demonstrates: SRE/metrics]
A4. Distributed tracing: OpenTelemetry spans (task -> step -> action -> LLM/CDP)
    so you can see the critical path of a 45-min run.  [Demonstrates: tracing]
A5. Cost accounting: $/run + $/successful-result; per-model breakdown; budget cap.

--- B. LATENCY / PERFORMANCE (the current ~45min pain) ---    [important]
B1. Faster model: gpt-4.1 (raise Azure TPM quota) — removes 75s timeouts,
    ~halves per-step time.  [Demonstrates: model/infra tradeoffs]
B2. Prompt/DOM size reduction: prune the DOM/accessibility tree sent per step
    (fewer tokens = faster + cheaper). Measure tokens/step before/after.
B3. Parallelize collection: extract N post overlays concurrently (asyncio /
    multiple CDP targets) instead of serial click->extract.  [Demonstrates: async perf]
B4. Streaming progress: stream step goals to Telegram (heartbeat #11) so
    perceived latency drops; optionally stream the final synthesis.
B5. Caching: cache search-result harvests / dedupe already-seen posts.

--- C. RELIABILITY / RESILIENCE ---
C1. fallback_llm: primary gpt-5.4/4.1 with automatic fallback on parse-fail/429.
C2. Retry+backoff for 429/timeouts (exponential, jittered) at the LLM layer.
C3. Circuit breaker: after K consecutive LLM failures, pause & alert instead of
    burning the step budget.  [Demonstrates: resilience patterns]
C4. Task persistence/resume: checkpoint collected records so a crash mid-run can
    resume (already writing workspace/collected/*.jsonl — build on it).

--- D. EVALUATION / QUALITY (AI-engineer signal) ---
D1. Eval harness: golden tasks + automated scoring (task success, groundedness,
    hallucinated-link rate). Run in CI.  [Demonstrates: LLM eval discipline]
D2. LLM-as-judge for groundedness: verify every returned link/summary is backed
    by a collected record (no fabrication).  [Demonstrates: hallucination guardrails]
D3. Regression suite: replay recorded transcripts; assert no perf/quality drop.
D4. Real unit/integration tests (pytest) for tools, skills matching, synthesizer.

--- E. SECURITY (important + often overlooked) ---
E1. Sandbox run_js: it executes arbitrary agent-authored JS — constrain/audit it.
E2. Secrets management: move keys out of .env into a manager (AWS Secrets Mgr /
    SOPS). NOTE: rotate the OpenAI key that was pasted in plaintext.
E3. Remote-view auth: token/expiry on the noVNC/screencast link (port 8770).
E4. Per-user authz + rate limiting on the Telegram bot.

--- F. SCALABILITY / ARCHITECTURE ---
F1. Job queue (Redis + worker) so multiple tasks/users run concurrently; the bot
    just enqueues.  [Demonstrates: distributed systems]
F2. Containerize (Docker) for reproducible dev/prod parity (defer per deploy doc).
F3. Multi-tenant: per-user profiles/sessions, isolation.
F4. Horizontal scale: stateless workers + shared job/result store.

--- G. PRODUCT / DX ---
G1. Web dashboard for run history, transcripts, metrics (reuse render_trajectory).
G2. Skill marketplace: more skills; self-improving skills from successful runs.
G3. Structured result output (JSON + Markdown), export.

RESUME FRAMING (pick 2-3 to go deep, quantify impact):
- "Built a human-in-the-loop web agent (Telegram + browser-use) with a CodeAct
  run_js tool and a two-phase collector/synthesizer, cutting a task from 40 steps
  /1 result to 14 grounded results."
- "Added LLM observability (Langfuse) + evals (groundedness, success-rate);
  reduced p50 latency Xx via model routing + DOM pruning + async extraction."
- "Shipped CI/CD (GitHub Actions -> AWS) with health-check auto-rollback."
Best depth-picks for interviews: A (observability) + B (latency) + D (evals).


 ----function:test linked-in -----



--- message queue---


====================================================================
PLATFORM ARCHITECTURE + LATENCY/COST (product view — for sell / open-source)
Reframe: this is NOT a scraper. It is an "authenticated browser-agent platform".
The MOAT = the human-in-the-loop login handoff -> the agent acts inside the user's
AUTHENTICATED sessions, reaching data/actions public APIs and naive agents cannot.
Optimize the whole architecture around that.
====================================================================

--- P1. ARCHITECTURE (make it a platform, not a monolith) ---
P1.1 Split control plane (stateless: API gateway, auth, orchestration, billing,
     observability) from data plane (stateful: browser + agent runtime, 1 per
     active session).  [Demonstrates: system decomposition, scalability]
P1.2 API-first. Telegram becomes ONE client of a public REST/WS/SDK API -> also
     enables web app / CLI / Slack / Zapier.  [Demonstrates: platform thinking]
P1.3 Session/Identity layer = the crown jewel. Per-user isolated persistent
     browser profiles (cookies/storage), encrypted at rest. Session lifecycle as
     a first-class state machine: needs-login -> human handoff -> active ->
     expiring -> re-auth. Anti-bot wall is a STATE, not a failure. Never store raw
     passwords — store the SESSION obtained via handoff.
     [Demonstrates: identity/session design, security posture]
P1.4 Multi-tenancy & isolation as a hard requirement (you run code inside people's
     logged-in accounts): sandboxed browser per session (container/net namespace),
     egress control, audit log of every action on the user's behalf, kill switch.
     [Demonstrates: security boundaries, blast-radius thinking]
P1.5 Job orchestration: queue (Redis/SQS) + workers with SESSION AFFINITY (route a
     task to the worker holding that user's warm browser). Concurrency + horizontal
     scale + graceful restart.  [Demonstrates: distributed systems]
P1.6 Extensibility for OSS adoption:
      - Tool/skill plugin API (versioned) — third parties extend without forking.
      - Pluggable LLM + BYO-key (generalize build_llm()).
      - Pluggable source/site adapters (generalize the harvester SourceAdapter).
P1.7 Self-hostable (OSS core, `docker compose`) + managed tier (browser pool,
     secrets mgr, dashboard). Standard OSS-SaaS split.

--- P2. LATENCY (attack the per-step round-trip, model-agnostic) ---
Measured reality (Langfuse): ~24s/step, prompt-bound; model swap did NOT help.
Per-step cost = LLM(prompt) + DOM serialize + CDP. Levers, biggest first:
P2.1 Context/DOM reduction (dominant lever): send a pruned accessibility tree +
     DOM DELTAS (what changed since last step), not the full 15-40k-token DOM each
     step. Hard token budget.  [Demonstrates: context engineering]
P2.2 Prompt caching: the big system prompt + stable page chrome are re-sent every
     step; use OpenAI/Anthropic prompt caching on the repeated prefix. Near-free.
P2.3 Model routing/tiering: small fast model for routine steps (scroll/click/
     extract), large model only for planning/ambiguity.  [Demonstrates: routing]
P2.4 Do more per LLM call (CodeAct): one run_js that loops over N items instead of
     one LLM round-trip per item.
P2.5 Warm browser pool: cold Chromium launch ~30s (BrowserStartEvent timeouts).
     Keep pre-launched, pre-authenticated browsers; check one out per job.
P2.6 Parallelism: async extraction across multiple tabs/CDP targets vs serial
     click->extract.
P2.7 Compiled/deterministic skills: record a successful trajectory once, REPLAY it
     without the LLM (LLM only on drift). 40-step LLM task -> ~0 LLM calls.
     [Demonstrates: caching/compilation, the biggest latency+cost cut]
P2.8 Streaming/perceived latency: stream step goals + partial results.

--- P3. COST (same knobs + infra) ---
P3.1 Tokens are ~90% of LLM cost -> P2.1 (DOM prune) + P2.2 (prompt cache) +
     P2.3 (tiering) are the cost levers too.
P3.2 Cost attribution + budgets: already trace $/run in Langfuse; add per-task /
     per-user budget caps and a "cheap mode" (smaller model, tighter DOM).
P3.3 Offline/batch synthesis on the cheapest model (Phase-2 synthesizer is already
     browser-less -> run it on a batch/cheap tier).
P3.4 Infra amortization: browser pooling + scale-to-zero workers + spot instances.
     A warm pool shared across users beats one browser per task.
P3.5 Semantic/result caching: dedupe repeated sub-tasks across users (respect
     isolation).
P3.6 Compiled skills (P2.7) eliminate LLM cost entirely for routine flows — the
     single biggest cost cut for high-volume tasks.

--- P4. PRODUCTIZATION GATES (can't skip — you hold authenticated sessions) ---
P4.1 Sandbox run_js (arbitrary agent-authored JS in a logged-in session) — audit +
     constrain.
P4.2 Secrets management: rotate the plaintext OpenAI key; vault session cookies.
P4.3 Audit log of every action taken on a user's behalf + kill switch.
P4.4 ToS/anti-bot posture: rate-limit, human-paced pacing, explicit per-site user
     consent.

Thesis (for reviewer/investor): "An API-first, multi-tenant platform for agents
that act inside users' authenticated browser sessions — with a warm browser pool,
DOM-delta context compression, model routing, and compiled deterministic skills to
keep latency and cost flat as usage scales."

--- 07/20/2026 ---
1. this agent dont have memory, but every /new starts a new context window(--> what happens if context window is full?) --> temporarily we dont need it --> prospond
2. how to scale --> docker deployment is a necessary. we can deploy multiple sandbox in one machine of server--> because differnt user cant share the same chorme core
3. --> be able to send the file back to user through telegram?  -- DONE (harvest sends the 面经.md as a Telegram document after the run; telegram_gateway reply_document)
4. current architecture is kind of ugly, can we Optimize it?
5. message queue? distributed system?
6. analyse past task log to improve UX / latency optimization: tighten end-to-end responsiveness — streaming progress,
faster login-handoff link delivery, prefetch/cache for common sources, and snappier
remote-view startup, so it feels instant on the phone.
7. if we want normal user to experience it quickly, we need -->
     1. a web application, including the front-end and back-end
     2. a server to patch the request to other sanboxes (may use message queues)
     3. it scaled, so mutiple servers/
     4. figure out how to calculate the cost.
     5. related database, redis, and so on...

8. 

--- 07/21/2026 ---
1. product positionning: lightweight personal agent focus on browser-use scenarios,  now my main purpose is to access infomation that needs to be logined or personal-related in batch. (you know, 
 help users to improve their ability to access infomations and make better decision.) but im sure this isnt the only use case.
   0. research --> hermes agent
   1. object --> make other use this easily, quickly, improve the system's Extensibility as the user number grows.
2. Scale, --> scale to test(test concurrently, ) --> what if i want to test task concurrently? we need to open diffent browser --> containerize it.
     1. it should be cloud-like, which means the agent should be able to migrate conveniently --> that's where database kicks in.
3. i dont know the ability boundaries now, --> test to find out
4. focus on one aspect --> i would like to 
5. reliability --> what if tasks fails? can we auto-regress it or keep it idempotent?

====================================================================
MINIMUM MULTI-USER (let friends use it) — design decided 07/21
====================================================================
Goal: a few friends use it CONCURRENTLY; grow resources as users grow; migratable;
reliable. This is DEPLOYMENT extensibility (multi-user), distinct from the Capability
plug-in extensibility (see 07_21_2026/architecture-supervisor-capabilities.md).

Driving constraint: users CANNOT share a Chrome core -> unit of isolation = one
browser per user -> control-plane / data-plane split.

  Control plane (1 small process): Gateway (allow-list friends) + Dispatcher
    (user -> session -> worker container; start on-demand, idle-stop) + task queue.
  Data plane (N containers, 1 per active user): agent + Chrome + profile volume + noVNC.
  Portable state = Postgres rows + per-user named profile volumes (containers = cattle).

M-1. Containerize the runtime (Dockerfile: agent + Chrome + Xvfb + noVNC). One
     container == one user's live session. (reuse run-aws.sh Xvfb+noVNC.) (= todo F2)
M-2. Per-user Chrome profile = NAMED VOLUME (or object store), keyed by user ->
     isolation + persistence + MIGRATABLE ("cloud-like": move volume+DB row = move session).
M-3. Control plane: extend gateway with auth (allow-list) + Dispatcher: user -> their
     container, start ON DEMAND, idle-stop to bound cost.
M-4. Postgres (migrate SQLite RunStore): users, sessions(user->volume/container),
     tasks(status, idempotency_key=request_id, result, artifacts).
M-5. Concurrency: DROP the global _busy. Per-USER single-flight (1 task/user); DIFFERENT
     users run in parallel (separate containers). This single change unlocks "test concurrently".
M-6. Reliability / idempotency (#5):
     - Task state machine in DB: queued -> running -> needs_login -> done/failed.
     - Idempotency key = request_id; a retry RESUMES via the ledger (harvest already
       dedup/resumable) instead of restarting.
     - Auto-retry TRANSIENT only (429/timeout/container crash) w/ backoff; TERMINAL =
       login-needed (wait for human) or ToS-block (stop). Requeue tasks stuck in 'running'.
M-7. Login handoff per user: each user logs into THEIR container's browser via THEIR
     token-gated noVNC link; never cross containers.

Honest limit: container-per-user is heavy (~0.5-1.5GB/Chrome) -> one VPS fits a handful.
Idle-stop + a cap NOW; multi-host / browser-pool / k8s autoscaling LATER (DB+volumes
leave the door open). Don't build it yet (Occam).

Ability boundaries (#3): build a PROBE SUITE (10-20 tasks across a few sites), run them
CONCURRENTLY in containers, record pass/fail/latency -> that IS the concurrency test AND
the capability map.

Build order: M-1 (containerize) -> M-5 (per-user concurrency, 1 host) -> M-4 (DB/state)
-> M-2 (profile volumes) -> M-3 (dispatcher/on-demand) -> M-6 (reliability).


---
questions:
1. Compute for the agent + can it be serverless (Alibaba FC)?  How do control plane
   and agents communicate — do we need an MQ?
   ANSWER (see scaleToEnterpriseLevel/platform-vision.md §6.5):
   - FC / serverless: resource-YES (custom container fits Chrome) but MODEL-NO — our
     agent is stateful + long-lived + human-in-the-loop, the opposite of FaaS. Run it as
     a LONG-LIVED container + persistent volume (Alibaba ECI / ASK / ECS + NAS). FC only
     for stateless helpers (Phase-2 synthesis, one-shot fetch).
   - Comms: friends/one-host = NO MQ (co-locate, direct HTTP + shared DB). Multi-host =
     3 channels: MQ for task dispatch (WITH per-user/affinity routing) + WebSocket/gRPC
     for live (progress, handoff link, /done) + DB/OSS for durable state. noVNC stream is
     DIRECT agent→user via tunnel (not through MQ). MQ is forced by "many un-addressable
     agents + bursty load + durable retries", not merely by "two servers". 